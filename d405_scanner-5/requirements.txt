# D405 Gussbauteil-Scanner – Python-Abhängigkeiten
# Installation: pip install -r requirements.txt

# ── Kernbibliotheken ──────────────────────────────────────────────────
pyrealsense2>=2.55.0        # Intel RealSense SDK (D405 Support)
open3d>=0.18.0              # Punktwolken, Mesh, ICP, Poisson

# ── Numerik & Visualisierung ─────────────────────────────────────────
numpy>=1.24.0
scipy>=1.11.0               # Für Interpolation und Filterung
matplotlib>=3.7.0           # Heatmaps und Diagramme

# ── Bildverarbeitung ─────────────────────────────────────────────────
Pillow>=10.0.0              # PNG/JPEG-Export

# ── Report ────────────────────────────────────────────────────────────
# HTML-Reports werden mit reinem Python erstellt (kein Jinja2 nötig)

# ── Entwicklung / Tests ───────────────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0

# ── Optionale Verbesserungen ─────────────────────────────────────────
# tqdm          # Fortschrittsbalken
# rich          # Schönere Console-Ausgabe
# opencv-python # Erweiterte Bildverarbeitung
